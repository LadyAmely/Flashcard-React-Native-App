import React from 'react';
import { Text} from 'react-native';
import {styles} from "./titleStyles.ts";


const Title: React.FC = () => {
    return <Text style={styles.title}>FlashLearn</Text>;
};


export default Title;
